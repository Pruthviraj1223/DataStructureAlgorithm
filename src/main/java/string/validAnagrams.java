package string;

public class validAnagrams {
    public static void main(String[] args) {

        // a lot solution from GFG

        // sort char array and compare it
        // hashmap , char -> count
        // 2 count arr. count1 and count2 and compare each other's count

        // (best) 1 count arr. first increase the count and decrease the count then check count is zero or not.

    }
}
