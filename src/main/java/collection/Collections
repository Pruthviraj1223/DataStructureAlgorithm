

        // Methods

        // addAll
        // binarySearch(List<? extends Comparable> list, T key)
        // copy (List<? super T> dest, List<? extends T> src)
        // nCopies(int n, T o)
        // disjoint (Collection<?> c1, Collection<?> c2)
        // frequency (Collection<?> c, Object o)
        // indexOfSubList (List<?> source, List<?> target) and lastIndexOfSubList (List<?> source, List<?> target)
        // max (Collection<? extends T> c) and min(Collection<? extends T> coll)
        // replaceAll (List<T> list, T oldVal, T newVal)
        // reverse(List<?> list)
        // rotate(List<?> list, int distance)
        // sort(List<T> list)
        // sort (List<T> list, Comparator<? super T> c)
        // swap(List<?> list, int i, int j)
        // synchronizedCollection(Collection<T> c)	 , synchronizedList(List<T> list)	, synchronizedMap(Map<K,V> m)
        // unmodifiableCollection(Collection<? extends T> c)	, unmodifiableList(List<? extends T> list)
